<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (C) 2010 The Android Open Source Project

     Licensed under the Apache License, Version 2.0 (the "License");
     you may not use this file except in compliance with the License.
     You may obtain a copy of the License at

          http://www.apache.org/licenses/LICENSE-2.0

     Unless required by applicable law or agreed to in writing, software
     distributed under the License is distributed on an "AS IS" BASIS,
     WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
     See the License for the specific language governing permissions and
     limitations under the License.
-->

<resources>

    <declare-styleable name="KeyboardBaseView">
        <!-- Default KeyboardView style. -->
        <attr name="keyboardViewStyle" format="reference" />

        <!-- KeyboardView background image. -->
        <attr name="keyboardBackground" format="reference" />
        
        <!-- KeyboardView background padding bottom. -->
        <attr name="keyboardPaddingBottom" format="dimension" />
        
        <!-- Image for the key. This image needs to be a StateListDrawable, with the following
             possible states: normal, pressed, checkable, checkable+pressed, checkable+checked,
             checkable+checked+pressed. -->
        <attr name="keyBackground" format="reference" />

        <!-- Size of the text for custom keys with some text and no icon. -->
        <attr name="labelTextSize" format="dimension" />
        
        <!-- Size of the text for character keys. -->
        <attr name="keyTextSize" format="dimension" />

        <!-- Color to use for the label in a key. -->
        <attr name="keyTextColor" format="color" />
        
        <attr name="keyTextStyle">
            <flag name="normal" value="0" />
            <flag name="bold" value="1" />
            <flag name="italic" value="2" />
        </attr>

        <!-- Color to use for the label in a modifier key. -->
        <attr name="keyModifierColor" format="color" />
        
        <!-- Size of the text for key hint. -->
        <attr name="keyHintTextSize" format="dimension" />

        <!-- Color to use for key hint labels. -->
        <attr name="keyHintColor" format="color" />

        <!-- Size of the text for key trail. -->
        <attr name="keyTrailTextSize" format="dimension" />

        <!-- Color to use for key trail labels. -->
        <attr name="keyTrailColor" format="color" />

        <!-- Color to use for the language bar label. -->
        <attr name="languagebarTextColor" format="color" />

        <!-- Text shadow color for language bar label. -->
        <attr name="languagebarShadowColor" format="color" />
        
        <!-- Space Auto-Completion Indicator icon. -->
        <attr name="spaceAutoCompletionIndicator" format="reference" />

        <!-- Drawable resource for key hint popup. -->
        <attr name="keyHintPopup" format="reference" />

        <!-- Layout resource for key press feedback.-->
        <attr name="keyPreviewLayout" format="reference" />

        <!-- Vertical offset of the key press feedback from the key. -->
        <attr name="keyPreviewOffset" format="dimension" />

        <!-- Height of the key press feedback popup. -->
        <attr name="keyPreviewHeight" format="dimension" />

        <!-- Background resource for key preview. -->
        <attr name="keyPreviewBackground" format="reference" />
      
        <!-- Background resource for space key (Language Switcher) preview. -->
        <attr name="keyPreviewSlideBackground" format="reference" />

        <!-- Hysteresis distance for key debouncing -->
        <attr name="keyHysteresisDistance" format="dimension" />

        <!-- Amount to offset the touch Y coordinate by, for bias correction. -->
        <attr name="verticalCorrection" format="dimension" />

        <!-- Layout resource for popup keyboards. -->
        <attr name="popupLayout" format="reference" />

        <!-- Text shadow color for key label. -->
        <attr name="shadowColor" format="color" />

        <!-- Text shadow radius for key label. -->
        <attr name="shadowRadius" format="float" />

        <!-- Background dimension amount for key. -->
        <attr name="backgroundDimAmount" format="float" />
        
        <attr name="candidateBackground" format="reference" />
        <attr name="candidateSelection" format="reference" />
        <attr name="candidateDivider" format="reference" />
        <attr name="candidateTextColorNormal" format="color" />
        <attr name="candidateTextColorRecommended" format="color" />
        <attr name="candidateTextColorOther" format="color" />

        <attr name="numHintTopPadding" format="dimension" />
        <attr name="numHintRightPadding" format="dimension" />
        <attr name="hintPopupBottomPadding" format="dimension" />
        <attr name="hintPopupRightPadding" format="dimension" />
    </declare-styleable>

</resources>
